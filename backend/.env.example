# GMI Cloud — agent brain (OpenAI-compatible serverless API)
GMI_API_KEY=
GMI_BASE_URL=https://api.gmi-serving.com/v1

# PartCrafter on GMI (dedicated endpoint or rented GPU). image -> parts.
PARTCRAFTER_URL=

# Fallback single-mesh generator
MESHY_API_KEY=

# CORS: comma-separated origins allowed to call this backend.
# Vercel preview deploys (*.vercel.app) are allowed by regex in main.py.
CORS_ORIGINS=http://localhost:3000
